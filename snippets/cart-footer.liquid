<form action="{{ routes.cart_url }}" method="post" id="CartFooterForm">
  {%- if settings.cart_show_note -%}
    <details class="accordion__item" style="border-top:1px solid var(--border);margin-bottom:1.6rem">
      <summary class="accordion__summary">
        {{ 'sections.cart.note' | t }}
        <span class="accordion__icon"></span>
      </summary>
      <div class="accordion__content">
        <label class="visually-hidden" for="CartNote">{{ 'sections.cart.note' | t }}</label>
        <textarea class="field__input" id="CartNote" name="note" rows="3">{{ cart.note }}</textarea>
      </div>
    </details>
  {%- endif -%}

  {%- if cart.cart_level_discount_applications.size > 0 -%}
    {%- for discount in cart.cart_level_discount_applications -%}
      <div class="cart__totals">
        <span>{{ discount.title }}</span>
        <span>&minus;{{ discount.total_allocated_amount | money }}</span>
      </div>
    {%- endfor -%}
  {%- endif -%}

  <div class="cart__totals">
    <span class="cart__total-final">{{ 'sections.cart.subtotal' | t }}</span>
    <span class="cart__total-final">{{ cart.total_price | money_with_currency }}</span>
  </div>

  <p class="cart__tax-note">{{ 'sections.cart.taxes_and_shipping_at_checkout' | t }}</p>

  {%- comment -%}
    The drawer is a preview, so its button leads to the cart page. Only the
    cart page itself submits to checkout.
  {%- endcomment -%}
  {%- if context == 'drawer' -%}
    <a class="button button--full" href="{{ routes.cart_url }}">
      {{ 'sections.cart.checkout' | t }}
    </a>
  {%- else -%}
    <button type="submit" name="checkout" class="button button--full">
      {{ 'sections.cart.checkout' | t }}
    </button>
  {%- endif -%}
</form>
